<h1>Songs</h1>
<tbody>
    <tr>
    <% @songs.each do |song| %>
        <td><%= song.title %></td>
        <td><%= song.released %></td>
        <td><%= song.release_year %></td>
        <td><%= song.artist_name %></td>
        <td><%= song.genre %></td>
    <% end %>
    <tr>
</tbody>
    